# The frontend calls relative API paths ('/auth/login', '/pipeline/status/...')
# which are proxied to the backend by Vite in dev (vite.config.ts) and by
# nginx in production (nginx.conf). There is currently no build-time env
# variable the frontend needs — this file is a placeholder for when one is.
